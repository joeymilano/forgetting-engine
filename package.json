{
  "name": "forgetting-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "The Forgetting Engine — 一个专门负责「忘记」的机器。",
  "scripts": {
    "dev": "vite",
    "dev:server": "node server.mjs",
    "start": "node server.mjs & vite",
    "build": "tsc --noEmit && vite build",
    "test:unit": "vitest run",
    "test": "npm run build && npm run test:unit && node --test test/deployment.test.mjs",
    "deploy": "npm test && npx wrangler pages deploy dist --project-name forgetting-engine",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "^5.4.5",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  }
}
